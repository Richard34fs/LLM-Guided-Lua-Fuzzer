function generate_code(str)
  local result = ""
  for i = 1, #str do
    result = result .. string.rep(string.sub(str, i, i), 20)
  end
  return result
end

local function generate_code(str)
  local result = ""
  for i = 1, #str do
    result = result .. string.rep(string.sub(str, i, i), 20)
  end
  return result
end

function generate_code(str)
  local result = ""
  for i = 1, #str do
    result = result .. string.rep(string.sub(str, i, i), 20)
  end
  return result
end