function generate_lua(str)
    local result = ""
    for i = 1, #str do
        result = result .. string.rep(str:sub(i, i), 5)
    end
    return string.gsub(result, "a", "")
end
function generate_lua(str)
    local result = ""
    for i = 1, #str do
        result = result .. string.rep(str:sub(i, i), 5)
    end
    return string.gsub(result, "a", "")
end
function generate_lua(str)
    local result = ""
    for i = 1, #str do
        result = result .. string.rep(str:sub(i, i), 5)
    end
    return string.gsub(result, "a", "")
end
function generate_lua(str)
    local result = ""
    for i = 1, #str do
        result = result .. string.rep(str:sub(i, i), 5)
    end
    return string.gsub(result, "a", "")
end